# Known Limitations

- This is a patch for the current Build 0.9.1 repository tree, not a standalone full-game archive.
- Phase V remains a work-in-progress endpoint.
- Mid-dialogue language switching may retain text already materialized for the active line, consistent with the previously recorded non-blocking limitation.
- Local Chromium runtime testing was unavailable because localhost pages are blocked by the execution environment's organization policy.
- Final acceptance requires the owner's Android/live-site test.
