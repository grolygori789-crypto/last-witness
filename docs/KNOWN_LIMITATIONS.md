# Known Limitations

- This candidate assumes the existing runtime already provides the shared helpers used by earlier phases (`show`, `runDialogue`, `manualSave`, `autoSave`, `LastWitnessChapter2Integration`, etc.).
- Phase V is not implemented here; the completion handoff opens a Chapter III WIP continuation card for Marina Bay.
